Puedes hacerlo con `git clone (url)`.
```
git clone https://...
```

Esto crea un directorio del mismo nombre del repositorio, inicializa un directorio .git en su interior, descarga toda la información de ese repositorio y saca una copia de trabajo de la última versión.