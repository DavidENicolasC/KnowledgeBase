Platform cache clears some space. When the partition limit is exceeded, Salesforce evicts cached data based on a Least Recently Used (LRU) algorithm.

Data is stored according with its [[Time-to-live]].