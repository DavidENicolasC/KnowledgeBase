Visualforce: Using a standard controller, retrieve the record Id from the ApexPages.StandardController object.
Lightning Web Components:
1. Expose the component to include it in record pages.
2. In Javascript, declare recordId as public property. The parent component (record Flexipage) populates the value.
3. Pass the recordId as a parameter in Apex method calls