Data to be searched must be indexed. Indexed searches are performed by first searching the indexes for appropiate records, then narrowing down the results based on access permissions, search limits, and other filters. This creates a result set, typically containing most relevant results. After the set reaches a predetermined size, remaining records are discarded. The result set is then used to retrieve the fields that a user sees.