It's the best way to query and process large data sets. You can query and process up to 50 million records using asynchronous batch Apex.

Doesn´t work in all cases, like as a Visualforce page needs to query more than 50,000 records.