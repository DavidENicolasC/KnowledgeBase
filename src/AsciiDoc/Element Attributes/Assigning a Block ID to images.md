You cannot currently define an ID on an inline image. Instead you need to place an inline anchor adjacent to it.

```
[[tiger-image]]image:tiger.png[Image of a tiger]
```

You can use instead the [[anchor Macro]].