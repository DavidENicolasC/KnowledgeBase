Define the built-in and user-defined settings and metadata that can be applied to an individual block element or inline element in a document (including [[Macro]]s).

Can also be defined on an [[include directive]].

May be:
- [[Positional Attribute]] (value only)
- [[Named Attribute]] (key/value pair).

Are defined directly on the element to which they apply using an [[Attribute list]].