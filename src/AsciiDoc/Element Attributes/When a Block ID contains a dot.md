You must define it using either a longhand assignment
```
id=classname.propertyname
```
or the anchor shorthand
```
[[attributes:id:::class­ name.propertyname]]
```


This is necessary since the `.` character in the shorthand syn­tax is the delimiter for a [[Role]], and thus gets misinterpreted as such.