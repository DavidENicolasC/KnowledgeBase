You can wrap attribute values for [[Declaring attribute values in multiple lines]]. There are two types:
- Soft wrapping - Uses [[Line Continuation]]s.
- Hard wrapping - Uses [[Hard line break replace]]s.