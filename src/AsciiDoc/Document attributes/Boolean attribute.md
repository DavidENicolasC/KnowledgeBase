If is defined, but not given a value, that is, set, it’s in the "on" state. If the attribute is not defined, that is, is not set, it’s in the "off" state.

You can indicate this with a leading (preferred) or trailing `!` added to the name.

```
:name-of-a-boolean-attribute:
```