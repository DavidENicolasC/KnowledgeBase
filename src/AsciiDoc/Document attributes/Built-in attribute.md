Provide access to read-only information about the document and its environment OR Allow the author to configure behavior of the [[AsciiDoc Processor]] for a whole document or select [[Region]]s.

Are effectively unordered.

Add, configure and control common features in a document. Many built-in attributes only take effect when defined in the document header with an attribute entry.

The [[Boolean attribute]] is a subgroup of this.

Are reserved and can’t be re-purposed for user-defined attribute names.

When attribute value empty, defaults to the default value.