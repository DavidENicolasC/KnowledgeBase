Replaces special characters fol­ lowed by attribute references, and is applied to the values of attribute entries, regardless of whether the entries are defined in the header or in the [[Document Body]].

Same group that gets applied to metadata lines (author and revision information) in the document header.