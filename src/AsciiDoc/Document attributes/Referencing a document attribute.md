Means replacing an [[attribute]] name with that attribute’s value. A [[document attribute]] can be referenced in the document using the syntax

```
{name}
```

where _name_ is the name of the attribute.