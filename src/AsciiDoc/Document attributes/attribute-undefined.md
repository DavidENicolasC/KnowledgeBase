Controls how an expression that undefines an attribute ([[Unsetting built-in and boolean document attributes]]) is handled.

Has the possible following values:
- **drop** - Substitute the expression with an empty string after processing it.
- **drop-line** - Drop the line that contains this expression.