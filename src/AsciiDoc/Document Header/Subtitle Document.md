Only partitioned for DocBook, EPUB 3, and PDF converters.

HTML 5 converter does not split this out the document title.

```
= Main Title: Subtitle
```

Only the last occurrence of the separator `:` is used for partitioning the title.