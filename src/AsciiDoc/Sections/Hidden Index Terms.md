For Hiding [[Index Term]]s, index entries should be directly adjacent to the paragraph content to which they apply.
```
=== Create a new Git repository

(((Repository, create)))
(((Create Git repository)))
To create a new git repository,
```