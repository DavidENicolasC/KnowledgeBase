Unset the `sectids` attribute.

```
:!sectids:
```


[[Custom Section ID]]s are still used even when is disabled.


You can disable it for only certain sections and discrete headings.

```
== ID generation on

:!sectids:
== ID generation off
:sectids: A
```