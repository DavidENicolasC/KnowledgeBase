Refers to the _Chapter_ word before the [[Chapter Title]].

Is assigned when the [[sectnums Attribute]] ([[Numbering sections]]) is also set.

You can modify it by using the `chapter-signifier` attribute.
```
:chapter-signifier: Peatükk
```

You can disable it.
```
:!chapter-signifier:
```