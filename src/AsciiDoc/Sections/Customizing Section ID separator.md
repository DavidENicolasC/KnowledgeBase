Use the `idseparator` attribute.

Customizing:
```
:idseparator:
```

Removing:
```
:idseparator:
```

When a document is rendered on [[GitHub]], is set to `-`. These settings are used to ensure that the IDs generated by GitHub match the IDs generated by [[Asciidoctor]].