 [[Index Term]] that appears in the flow of text (i.e., a visible term) and in the index.
```
indexterm2:[<primary>]
((<primary>))
```

Can only be used to define a primary entry and is case sensitive.