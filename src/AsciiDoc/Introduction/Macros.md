Syntax for representing non-text elements or syntax that expands into text using the provided [[Metadata]].

Consists of a Name, an optional Target, and an optional [[Attribute list]].

There are two types:
- [[Block macro]]
- [[Inline macro]]