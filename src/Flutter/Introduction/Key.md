Control which [[Widget]]s the [[Framework]] matches up with other widgets when a widget rebuilds.

Are most useful in widgets that build many instances of the same type of widget. For example, the [[ShoppingList]] widget, which builds just enough [[ShoppingListItem]] instances to fill its visible region.