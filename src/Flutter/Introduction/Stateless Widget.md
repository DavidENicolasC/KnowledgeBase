Receive arguments from their parent [[Widget]], which they store in [[final]] [[Member variable]]s.

When a widget is asked to [[build()]], it uses these stored values to derive new arguments for the widgets it creates.