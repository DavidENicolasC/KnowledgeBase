Doesn't have a visual representation but instead detects gestures made by the user.

You can use it to detect a variety of input gestures, including taps, drags, and scales.

When user taps the [[Container]], calls its [[onTap()]] [[Callback]].