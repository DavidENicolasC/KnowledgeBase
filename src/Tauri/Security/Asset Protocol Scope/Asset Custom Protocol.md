Used for Tauri can serve files from disk into the WebView.

Used for example, when you use [[convertFileSrc]] in the frontend.

is controlled by **`app.security.assetProtocol`** in the [[Tauri Configuration File]].

You must set an [[Asset Protocol Scope]].