Can ship with any number of pieces of an optional [[JavaScript API]] and [[Rust API]] so that webviews can control the system via [[Message Passing]].

Only contains the code and assets specific for that app and doesn’t need to bundle a browser engine with every app.

Are maked after [[Create a Tauri Project]]

Dev code has the [[Tauri Project Structure]]