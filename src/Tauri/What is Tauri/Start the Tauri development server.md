After [[Create a Tauri Project]], you can navigate into your project’s folder, install dependencies, and then use the [[Tauri CLI]] to start the development server, depending on the selected [[Package Manager]]:

npm:
```
cd tauri-app
npm install
npm run tauri dev
```