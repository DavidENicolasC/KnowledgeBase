Is not a lightweight kernel wrapper. Instead, it directly uses [[WRY]] and [[TAO]] to do the heavy lifting in making system calls to the OS.

Is not a VM or virtualized environment. Instead, it is an application toolkit that allows making Webview OS applications.