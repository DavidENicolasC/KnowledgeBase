The secure [[JavaScript]] code that is injected by the [[Isolation Pattern]].

Intercepts _**all**_ messages from the frontend.

Even work with always-on APIs such as [[Javascript Namespace Event]]s.

Use the sandboxing feature of `<iframe>`s to run the JavaScript securely alongside the main frontend application.