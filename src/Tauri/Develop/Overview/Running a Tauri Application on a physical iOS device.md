you must also provide the `--host` argument and your development server must use the `process.env.TAURI_DEV_HOST` value as host.

For example, with [[npm]]:
```
npm run tauri [android|ios] dev --open --host
```

