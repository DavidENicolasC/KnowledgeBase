1. run the `tauri dev` command.
For example, in [[npm]]:
```
npm run tauri dev
```

The first time you run this command, the Rust package manager may need **several minutes** to download and build all the required packages.

2. You can make changes to your web app, and if your tooling supports it, the webview should update automatically, just like a browser.
3. You can [[Use the Web Inspector to Debug the Tauri app]].
4. 