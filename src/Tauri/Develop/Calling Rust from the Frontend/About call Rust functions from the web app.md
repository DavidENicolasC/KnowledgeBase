Tauri provides a simple yet powerful `command` system for calling [[Rust function]]s from your web app. Commands can accept [[arguments]] and return values. They can also return errors and be `async`.

Consider [[Where to define Rust commands for your Frontend]].