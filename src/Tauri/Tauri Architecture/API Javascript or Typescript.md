[[Typescript]] library that creates `cjs` and `esm` JavaScript endpoints for you to import into your frontend framework so that the Webview can call and listen to backend activity. 

Ships in pure typescript, because for some frameworks this is more optimal.

Uses the message passing of webviews to their hosts.