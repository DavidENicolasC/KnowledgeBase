Piece of the UI (user interface) that has its own logic and appearance.

Can be as small as a button, or as large as an entire page.

Are JavaScript functions that return [[JSX Markup]]:
```
function MyButton() {
	return (
		<button>I'm a button</button>
	);
}
```

