[[HTML]] variant. It is optional, but most React projects use JSX for its convenience.

Is stricter than HTML.

You have to close tags like `<br />`.

Your component also can’t return multiple [[JSX Tag]]s.

You have to wrap them into a shared parent, like a `<div>...</div>` or an [[Empty Wrapper]] `<>...</>`:

You can [[Display Javascript attributes on JSX]].