Cache partitions are distributed to subscribers as part of the app's package. They aren´t automatically added as dependent components.

- The partitions added must be non-default partitions.
- User must to enable your app to access the installed cache partitions for your namespace in order to install the cache partitions for the installed package
- Consider the [[Platform cache default availability]].