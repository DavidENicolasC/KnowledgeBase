1. In Workbench REST Explorer, select **PUT** or **PATCH**.
2. For the URI, enter `/services/apexrest/Cases/`.
3. The Apex method expects the field values to be passed in the request body. Replace `<Record ID>` with the ID of the case record into the request body.
4. Click **Execute**.