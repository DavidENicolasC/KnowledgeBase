1. In Workbench, select **GET**.
2. Enter the URI `/services/apexrest/Cases/<Record ID>`, replacing `<Record ID>` with the ID of the record that you want to retrieve.
3. Click **Execute**. This invocation calls the method associated with the GET HTTP method.
4. To view the response returned, click **Show Raw Response**.