This is a lightweight programming model based on the web components standards.

The components are isolated and secured by the Lightning Locker service. This enforces Javascript strict mode, provides DOM access containment, uses secure versions of global projects, and follows the Content Security Policy standard to prevent code injection attacks.