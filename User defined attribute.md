Serve as a powerful explanation tool.

Are stored in the order in which they are defined.

Any attribute that the author sets that isn’t reserved by the [[AsciiDoc]] language or an [[extension]].

Most of the time, user-defined attributes are used as a text replacement tool. Allow the author to define named, reusable content. 